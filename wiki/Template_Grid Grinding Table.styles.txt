/* Grinding-widget styling, loaded via <templatestyles> from [[Template:Grid Grinding Table]] and
   [[Module:Grinding]]. Complements [[Template:Grid Crafting Table/styles.css]] (the shared
   .logi-grid panel, slots, and arrow). Layout: input top-left with its experience below; the
   arrow spans both rows; the primary output top-right with its ×count to the left; an optional
   byproduct slot beneath the output with its chance to the left. */
.logi-grind-grid {
	border-collapse: separate;
	border-spacing: 3px;
	background: transparent;
}
/* ×count sits to the right of the primary output; the byproduct chance sits under the arrow,
   right-aligned toward the byproduct slot below the output. Both collapse to nothing when absent. */
.logi-grind-count {
	width: 2.6em;          /* fixed so the widget is the same width with or without a ×count */
	vertical-align: middle;
	text-align: left;
	padding-left: 4px;
	font-weight: bold;
	white-space: nowrap;
}
.logi-grind-chance {
	vertical-align: middle;
	text-align: right;
	padding-right: 3px;
	/* "Minecraft" must be installed site-wide (MediaWiki:Common.css @font-face); falls back to monospace */
	font-family: "Minecraft", monospace;
	color: #4d4d4d;
	white-space: nowrap;
}
.logi-grind-xp {
	text-align: center;
	font-family: "Minecraft", monospace;
	font-size: 90%;
	color: #000000;       /* the orb carries the colour, so the value stays black */
	padding-top: 2px;
	white-space: nowrap;
}
/* center the orb and value on the same line; .mw-file-element is MediaWiki's <img> */
.logi-grind-xp .mw-file-element,
.logi-grind-xp .logi-xp-val {
	vertical-align: middle;
}
.logi-grind-xp .logi-xp-val {
	margin-left: 2px;
}
