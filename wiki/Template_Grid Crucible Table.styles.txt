/* Crucible-widget styling, loaded via <templatestyles> from [[Template:Grid Crucible Table]] and
   [[Module:Crucible]]. Complements [[Template:Grid Crafting Table/styles.css]] (the shared
   .logi-grid panel, slots, and arrow). Layout: a single input on the left, the arrow, the fluid
   output, and its produced amount in millibuckets to the right of the output slot. */
.logi-crucible-grid {
	border-collapse: separate;
	border-spacing: 3px;
	background: transparent;
}
/* The mB amount sits to the right of the fluid output, fixed-width so the widget keeps a
   consistent size across recipes. */
.logi-crucible-amount {
	width: 4.2em;
	vertical-align: middle;
	text-align: left;
	padding-left: 4px;
	/* "Minecraft" must be installed site-wide (MediaWiki:Common.css @font-face); falls back to monospace */
	font-family: "Minecraft", monospace;
	color: #4d4d4d;
	white-space: nowrap;
}
