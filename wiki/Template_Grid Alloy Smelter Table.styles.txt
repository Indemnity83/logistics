/* Alloy-Smelter-widget styling, loaded via <templatestyles> from
   [[Template:Grid Alloy Smelter Table]] and [[Module:AlloySmelter]]. Complements
   [[Template:Grid Crafting Table/styles.css]] (the shared .logi-grid panel, slots, and arrow).
   Layout: two stacked inputs on the left, the arrow spanning both rows, the primary output
   top-right with its ×count, and a byproduct slot below it with its chance. */
.logi-alloy-grid {
	border-collapse: separate;
	border-spacing: 3px;
	background: transparent;
}
/* ×count sits to the right of the primary output; both are fixed-width so the widget keeps a
   consistent size with or without a count/byproduct. */
.logi-alloy-count {
	width: 2.6em;
	vertical-align: middle;
	text-align: left;
	padding-left: 4px;
	font-weight: bold;
	white-space: nowrap;
}
.logi-alloy-chance {
	width: 2.6em;
	vertical-align: middle;
	text-align: left;
	padding-left: 4px;
	/* "Minecraft" must be installed site-wide (MediaWiki:Common.css @font-face); falls back to monospace */
	font-family: "Minecraft", monospace;
	color: #4d4d4d;
	white-space: nowrap;
}
