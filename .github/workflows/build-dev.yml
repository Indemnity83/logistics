name: Build Dev

on:
  pull_request:
    branches:
      - main
    types: [opened, synchronize, reopened, ready_for_review]

permissions:
  contents: read

jobs:
  build-dev:
    if: >-
      github.actor == 'release-please[bot]' ||
      startsWith(github.head_ref, 'release-please--')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Compute dev version
        id: version
        shell: bash
        run: |
          # Release Please PRs update version.txt with the next release version
          NEXT_VERSION="$(cat version.txt)"

          # Get short SHA
          SHA7="$(git rev-parse --short=7 HEAD)"

          # Construct dev version based on the proposed next release version
          DEV_VERSION="${NEXT_VERSION}-dev.${SHA7}"

          echo "mod_version=$DEV_VERSION" >> "$GITHUB_OUTPUT"
          echo "📦 Dev version: $DEV_VERSION"

      - name: Build
        run: ./gradlew clean build --no-daemon
        env:
          MOD_VERSION: ${{ steps.version.outputs.mod_version }}

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: logistics-${{ steps.version.outputs.mod_version }}
          path: |
            build/libs/*.jar
            !build/libs/*-sources.jar
            !build/libs/*-javadoc.jar
            !build/libs/*-dev.jar

      # Uncomment when we're ready to publish dev builds to mod platforms
      # - name: Publish (alpha)
      #   uses: Kir-Antipov/mc-publish@v3
      #   with:
      #     version: ${{ steps.version.outputs.mod_version }}
      #     version-type: alpha
      #     changelog: "Automated dev build from commit ${{ github.sha }}"
      #
      #     files: |
      #       build/libs/!(*-@(dev|sources|javadoc)).jar
      #       build/libs/*-sources.jar
      #
      #     modrinth-id: logistics
      #     modrinth-featured: false
      #     modrinth-token: ${{ secrets.MODRINTH_TOKEN }}
      #
      #     # curseforge-id: logistics-automation
      #     # curseforge-token: ${{ secrets.CURSEFORGE_TOKEN }}
      #
      #     github-token: ${{ secrets.GITHUB_TOKEN }}
